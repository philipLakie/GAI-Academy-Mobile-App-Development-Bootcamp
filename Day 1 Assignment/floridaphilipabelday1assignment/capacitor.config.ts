import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.floridaphilipabelday1assignment',
  appName: 'floridaphilipabelday1assignment',
  webDir: 'dist'
};

export default config;
